# -*- coding: utf-8 -*-
"""
Created on Mon Mar 24 01:16:47 2025

@author: user
"""

# =============================================================================
# np.hstack() 和 np.concatenate(axis=1) 都是將數組按水平方向合併。
# 
# np.column_stack() 是一個簡化版本，專門用於將一維數組按列方向合併。
# 
# np.vstack() 是用來將數組按垂直方向（行方向）堆疊。
# 
# np.append() 更常用於添加元素，但可以用來模擬合併操作，並且可以指定 axis 參數。
# =============================================================================
